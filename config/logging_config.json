{
  "version": "1.00",
  "level": "INFO",
  "format": "[%(asctime)s] %(levelname)s %(name)s: %(message)s",
  "datefmt": "%Y-%m-%d %H:%M:%S",
  "log_dir": "logs",
  "files": {
    "prepare": "prepare_models.log",
    "benchmark": "benchmark.log"
  }
}
